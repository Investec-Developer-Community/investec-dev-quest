# Hint 2 — Count consecutive tool names

Track a running streak when the current `toolName` equals the previous one.

Reset streak when tool name changes. If streak reaches threshold, return `loopDetected: true`.
